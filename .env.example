# Client-safe values use NEXT_PUBLIC_ only when they are intentionally public.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Vercel demo-session HMAC secret (use a long random value in production)
PFL_SESSION_SECRET=
